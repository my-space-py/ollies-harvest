# 올리의 수확 — 구현 현황 및 TODO

이 문서는 저장소 전체 파일(`backend/`, `frontend/`, `GAME_DESIGN.md`, `README.md`)을 읽고 정리한
"지금까지 구현된 내용"과 "앞으로 구현해야 할 내용"입니다. 판단이 필요한 지점은 사용자에게 확인 후 반영했습니다.

## 1. 지금까지 구현된 내용

### 1.1 백엔드 (`backend/`)
- FastAPI 기반 인증 API: `POST /signup`, `POST /login`, `GET /`
- SQLite(`app.db`) + SQLAlchemy ORM, `User` 모델(아이디, 닉네임, bcrypt 해시 비밀번호)
- CORS 설정으로 프론트 정적 서버(4174 포트) 요청 허용
- **(2026-08-17 추가)** 게임 진행 데이터 저장 API: `GameSave` 모델, `GET/PUT /save` (4.3 참고)
- **(2026-08-17 추가)** 랭킹 API: `GET /leaderboard` — 누적 쌀 소비량 기준 전체 사용자 순위 (4.2 참고)
- **(2026-08-17 추가)** 친구 API: `Friendship` 모델, `POST /friends/request`·`GET /friends/requests`·`POST /friends/accept`·`POST /friends/decline`·`GET /friends` (4.2 참고)

### 1.2 프론트엔드 핵심 로직 (`frontend/game.js`)
- 클릭 수확 / 자동 수확(기본 15g/s) 및 이펙트(팝업, 파티클, 사운드)
- 업그레이드 6종 (생산 3, 클릭 1, 소비 2) 구매 로직과 비용 증가율
- 레시피 5종(밥·김밥·떡·쌀빵·누룽지) 소비 로직: 쌀알 소비, 누적 소비량 증가, 인기도/경험치 보상, 타임 버프, 영구 버프
- 미션 5종, 소비량 마일스톤 5단계 로직
- 작은 쌀 축제 달성 조건 체크 및 개최 다이얼로그
- 레벨/경험치 시스템, 레벨업 이펙트
- `localStorage` 자동 저장(15초 간격, 언로드 시), 오프라인 보상(최대 4시간) — **(2026-08-17)** 계정별로 키를 분리(`getStorageKey()`)하고, 로그인 시 서버 세이브와 동기화(`syncFromServer`/`pushToServer`)
- 효과음 On/Off 토글 (Web Audio API로 생성한 톤, 실제 음원 파일 없음)
- Canvas 기반 논밭 배경 애니메이션, 성장 단계에 따른 배경 전환(농장/마을/축제)
- 모바일/데스크톱 반응형 레이아웃, 하단 소프트키 네비게이션(홈/업그레이드/미션/레시피/통계/랭킹/친구 — 7탭, 2026-08-17 기준)

### 1.3 인증 화면 (`frontend/auth.html`, `js/auth.js`, `css/auth.css`)
- 로그인/회원가입 폼 전환, 백엔드 API 연동
- 로그인 성공 시 `localStorage("currentUser")`에 사용자 정보 저장 후 `index.html`로 이동
- `index.html`은 `currentUser`가 없으면 `auth.html`로 강제 리다이렉트, 로그아웃 버튼 제공

### 1.4 에셋
- 배경 3종, 캐릭터 6종, 작물/오브젝트, 이펙트 5종, 레시피 5종, 성장 단계 7종, UI 아이콘 8종, 업그레이드 6종 아이콘 등
- `GAME_DESIGN.md` 23장의 필요 에셋 목록과 대체로 일치

## 2. 기획 대비 확인된 불일치 (구현은 됐지만 화면에서 막혀 있는 부분)

> **(2026-08-17)** 아래 항목은 최초 분석 시점의 기록이며, 이 문서의 4장 작업을 통해 전부 해결되었습니다. 최종 검증 결과는 6장 참고.

- **레시피/누적 소비량/인기도 UI가 코드상 전부 숨김 처리**되어 있었음
  - `index.html`: 누적 쌀 소비량·인기도 표시 div에 `hidden` 속성
  - `aside.panel`(레시피 목록, 마일스톤 목록)이 `hidden` 속성 + CSS `display: none`으로 이중으로 숨겨짐
  - 즉, `useRecipe()` 로직은 존재하지만 **현재 화면에서는 레시피를 실행할 진입점이 없어 게임의 핵심 루프(소비)를 플레이할 수 없는 상태**였음
- 하단 네비게이션에 기획서에 없는 **'랭킹'/'친구' 탭**이 있고(현재는 "준비 중" placeholder), 기획서 17.6에 있는 **'레시피'/'통계' 탭은 없음**
- 퀵메뉴(창고/이벤트/공지/오늘의 미션) 4개 다이얼로그 모두 "준비 중" placeholder — 실제 내용 없음
- 게임 진행 데이터는 여전히 `localStorage`에만 저장되어 로그인 계정과 연동되지 않음

## 3. 사용자 확인 결과 (2026-08-17)

| 항목 | 결정 |
| --- | --- |
| 레시피/소비량/인기도 UI | **복원·재구현 필요** (최우선) |
| 랭킹/친구 탭 | **실제 기능 구현 예정** (기획서에 없던 신규 범위이므로 별도 기획 확정 필요) |
| 서버 저장 연동 | **포함** — 계정 기준으로 게임 데이터 저장/동기화 구현 |
| 출석 보상 | **포함** |
| 통계 화면 | **포함** |
| 실제 사운드 파일 | **제외** (Web Audio 생성 톤 유지) |

## 4. TODO

### 4.1 [최우선] 레시피 소비 UI 복원
- [x] 누적 쌀 소비량 / 인기도 hero-stats 표시 `hidden` 해제
- [x] 레시피 목록을 실제 플레이 화면에서 접근 가능하도록 복원 — 하단 네비게이션에 '레시피' 탭(`#recipeScreen`) 신설, 업그레이드/미션 탭과 동일한 패턴으로 구현 (사용자 확인 완료)
- [x] ~~마일스톤 UI 노출~~ → 4.5 통계 화면으로 이동, 완료 (아래 4.5 참고)
- [ ] 작은 쌀 축제 목표 카드(`bottom-cards`) 노출 여부 재검토 — 사용자 확인: 나중에 따로 처리 (보류)
- [x] 복원 후 "쌀 수확 → 레시피 소비 → 누적 소비량/인기도 상승" 전체 루프가 화면에서 실제로 동작하는지 확인 — 브라우저에서 밥 짓기 실행 후 누적 소비량 150g, 인기도 3 반영 확인

### 4.2 하단 네비게이션 정리 — 레시피/통계/랭킹/친구
- [x] '레시피' 탭 신설 여부 및 배치 결정 — 하단 네비 전용 탭으로 신설 완료 (4.1 참고)
- [x] '통계' 탭/화면 신설 — 보유 쌀알, 누적 소비량, 인기도, 레벨, 경험치, 업그레이드 보유 현황(6종), 소비 마일스톤 목록으로 구성 완료 (사용자 확인: '오늘의 소비량'은 일일 리셋 로직이 필요해 이번 범위에서 제외)
- [x] '랭킹' 기능 설계 및 구현 완료 — 사용자 확인: 누적 쌀 소비량 기준, 서버 전체 사용자 대상
  - 백엔드: `GET /leaderboard` 추가 (`backend/main.py`, `backend/schemas.py`) — `GameSave.data`의 JSON을 파싱해 `consumed` 기준 내림차순 정렬, 상위 N명 반환
  - 프론트: 랭킹 탭 진입 시 `loadLeaderboard()`가 호출되어 실시간 조회 (`frontend/game.js`, `frontend/index.html`) — 본인 항목은 "(나)" 표시
  - 두 테스트 계정(소비량 8kg/2.5kg)으로 정렬 순서, 본인 표시, 빈 랭킹 상태 메시지까지 브라우저에서 확인 완료, 테스트 데이터 정리함
- [x] '친구' 기능 설계 및 구현 완료 — 사용자 확인: 요청 → 수락 플로우, 화면은 친구 순위표만(상호작용 없음)
  - 백엔드: `Friendship` 모델(`requester_id`/`addressee_id`/`status`) 추가, `POST /friends/request`(아이디로 요청, 자기 자신·중복·이미 친구 방지), `GET /friends/requests`(받은 대기 요청), `POST /friends/accept`, `POST /friends/decline`, `GET /friends`(수락된 친구를 누적 소비량 내림차순으로 반환) 구현
  - 프론트: 친구 탭에 아이디 입력 폼, 받은 요청 목록(수락/거절 버튼), 친구 순위표를 구성 (`frontend/index.html`, `frontend/game.js`)
  - curl로 요청/중복 차단/수락/거절/양방향 친구 목록 확인 후, 브라우저에서 폼 제출·수락 버튼 클릭까지 실제 UI로 재확인, 테스트 계정 정리함
  - 저장 API와 동일하게 `user_id`를 별도 인증 없이 신뢰함 (TODO.md 4.3의 보안 관련 사용자 결정과 동일한 범위)

### 4.3 백엔드 확장 — 게임 데이터 서버 저장/동기화 [완료]
- [x] 게임 상태 저장용 DB 모델 추가 — `backend/models.py`에 `GameSave` 모델 추가 (`user_id` 1:1, 게임 상태 전체를 JSON 문자열 `data` 컬럼에 통짜 저장, 필드별 컬럼화 대신 프론트 localStorage 객체를 그대로 직렬화)
- [x] 세이브 조회/저장 API 추가 — `GET /save?user_id=`, `PUT /save`(upsert) 구현, curl로 생성/조회/갱신 확인 완료
  - **보안 관련 사용자 결정**: `user_id`를 별도 토큰 검증 없이 그대로 신뢰하기로 결정함(간단하지만 다른 사람의 user_id를 알면 저장 데이터를 덮어쓸 수 있는 보안 위험 있음, 공모전 프로토타입 범위의 의도적 단순화). `backend/main.py`에 경고 주석 남겨둠
- [x] 프론트: 로그인 성공 시 서버 세이브 로드(`syncFromServer`), 저장 시점(수동 저장/15초 주기/언로드 시)에 서버로 반영(`pushToServer`) — `frontend/game.js`
- [x] localStorage는 오프라인 폴백/캐시 용도로 유지 — 서버 연결 실패 시에도 로컬 저장으로 계속 플레이 가능
- [x] (구현 중 발견/수정) 로컬 저장 키가 계정별로 분리되어 있지 않아 같은 브라우저의 다른 계정 진행 데이터가 섞이는 문제 발견 → `getStorageKey()`로 `user.id`를 키에 포함하도록 수정
- [x] (구현 중 발견/수정) 로컬 저장 기록이 없는 새 기기/브라우저에서는 기본 상태의 `lastSavedAt`이 항상 "방금"으로 계산되어 서버가 더 오래된 시각이라 판단, 서버 데이터를 절대 불러오지 못하는 로직 버그 발견 → `hasLocalSave` 플래그로 실제 로컬 저장 여부를 구분하도록 수정
- [x] 브라우저에서 회원가입 → 로그인 → 플레이 → 저장 → (같은 계정으로 새 기기 가정 재로그인) → 서버 데이터 정상 로드까지 전체 흐름 검증 완료, 테스트 계정/데이터는 정리함
- [ ] (범위 밖 발견 사항, 별도 처리 필요) 회원가입/로그인 실패 시 에러 메시지가 `[object Object]`로 표시되는 기존 버그 발견 — `frontend/js/auth.js`의 `showMessage(data.detail)`가 FastAPI 검증 오류 배열을 문자열로 변환하지 않음

### 4.4 출석 보상 시스템 (기획서 12.7)
- [ ] 7일 출석판 UI 신규 구현 (퀵메뉴 '오늘의 미션' 다이얼로그 자리를 활용할지, 별도 진입점을 만들지 결정 필요)
- [ ] 출석 기록 상태를 저장 데이터(로컬 및 4.3 서버 저장)에 추가

### 4.5 통계 화면
- [x] 통계 화면 구현 완료 — `#statsScreen` 신설, `renderStatsSummary()` / `renderStatsUpgrades()` 추가, 기존 `#milestoneSection`의 마일스톤 목록을 이 화면으로 이동. 브라우저에서 요약 지표/업그레이드 보유 현황/마일스톤 진행률 모두 정상 렌더링 확인

### 4.6 정리가 필요한 기존 placeholder
- [ ] 퀵메뉴 '창고' 다이얼로그 실제 내용 정의 및 구현 — 기획서에 상세 사양 없음, 용도(보유 아이템/재화 열람 등) 확인 필요
- [ ] 퀵메뉴 '이벤트' 다이얼로그 — 기획서에 없는 개념, 용도 확인 필요
- [ ] 퀵메뉴 '공지' 다이얼로그 — 기획서에 없는 개념, 용도 확인 필요 (출석 보상과 별개로 유지할지)

### 4.7 기타 기술 이슈
- [ ] `backend/requirements.txt` 파일이 UTF-16으로 저장되어 있어 글자 사이에 공백처럼 보이는 인코딩 깨짐이 있음 — `pip install -r requirements.txt`가 정상 동작하는지 확인 필요
- [ ] `frontend/js/auth.js`의 회원가입/로그인 에러 메시지가 `[object Object]`로 표시되는 버그 — FastAPI가 422 검증 오류 시 `detail`을 배열(예: `[{msg: "...", loc: [...]}]`)로 반환하는데 `showMessage(data.detail)`이 이를 문자열로 변환하지 않고 그대로 DOM에 대입함. 4.3 작업 중 발견 (아이디 20자 제한 초과 테스트 중 확인)

## 5. 명시적으로 이번 범위에서 제외
- 실제 사운드 파일(음원) 적용 — 현재 Web Audio로 생성하는 간단한 톤 유지

## 6. 최종 통합 검증 (2026-08-17)

4장에서 완료 표시한 기능들을 새 테스트 계정 2개(`finalcheck1`, `finalcheck2`)로 처음부터 끝까지 이어서 재검증했습니다. 확인한 흐름:

- 회원가입 → 로그인 → 홈 화면 진입, 콘솔 에러 없음
- 클릭 수확 → 첫 수확 미션 달성/수령 → 업그레이드(좋은 볍씨) 구매 후 초당 수확 반영 → 레시피(밥 짓기) 실행 → 누적 소비량·인기도 갱신
- 통계 탭에서 보유 쌀알/누적 소비량/인기도/레벨·경험치/업그레이드 보유 현황/마일스톤 진행률이 홈·레시피 탭과 일치하는지 확인
- 랭킹 탭에서 본인이 누적 소비량 기준으로 정렬되어 표시되고 "(나)" 표시 확인
- 친구 탭에서 실제 아이디로 요청 전송 → 상대 계정으로 로그인 후 수락 버튼 클릭 → 양쪽 모두 친구 순위표에 정확한 소비량으로 반영
- 저장 후 `localStorage` 초기화 + 재로그인(새 기기 가정)으로 서버에서 진행 데이터가 정확히 복원되는지 확인 (오프라인 경과 시간만큼 쌀알 추가 반영도 정상)

**검증 중 발견한 사항 (버그 아님, 참고용 기록):**
- 저장(`저장` 버튼) 직후 0.1~0.2초 이내에 랭킹/친구 화면을 열면, `pushToServer()`가 비동기(fire-and-forget)라 서버 반영이 완료되기 전이라 일시적으로 이전 값이 보일 수 있습니다. 곧바로 다시 열면 정상적으로 최신 값이 표시됩니다. 실제 사용자의 클릭 간격에서는 사실상 발생하지 않는 수준이라 별도 수정은 하지 않았습니다.
- 테스트 중 브라우저 자동화 도구 자체의 일시적인 렌더링 지연으로 첫 클릭이 반영되지 않는 것처럼 보인 경우가 있었으나, 재클릭 시 정상 동작하고 실제 앱 코드와는 무관한 것으로 확인했습니다.

검증에 사용한 테스트 계정과 데이터는 모두 정리했으며, DB에는 원래 있던 실제 계정(`ttest`, id 1)만 남아 있습니다.
