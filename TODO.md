# 올리의 수확 — 구현 현황 및 TODO

> **2026-09-01 전면 재검증**: 저장소 코드 전체(`backend/`, `frontend/`, `GAME_DESIGN.md`)를 다시 읽고, 백엔드(uvicorn)·프론트엔드(정적 서버)를 실제로 띄운 뒤 브라우저로 회원가입 → 로그인 → 수확 → 업그레이드 → 레시피 소비 → 미션 → 통계 → 랭킹 → 친구 요청/수락 → 레벨업(LV.12까지)까지 전체 플레이 흐름을 직접 조작해서 확인했다. 2026-08-17 기록 이후 레벨/장비/부스터/미션/랭킹/친구 시스템이 상당 부분 새로 구현되었는데 이 문서에는 반영되어 있지 않았어서, 아래 1장·4장을 현재 코드 기준으로 다시 정리했다. 과거 결정 기록은 7장(부록)에 그대로 보존한다.
>
> **2026-09-01 추가 작업**: 출석 보상 시스템(3.1절)을 신규 구현하고, 기존 기능 전체에 대한 회귀 테스트(2.2절)를 한 차례 더 수행했다. 회귀는 발견되지 않았다.

## 1. 지금까지 구현된 내용 (2026-09-01 기준 재확인)

### 1.1 백엔드 (`backend/`)
- FastAPI 기반 인증 API: `POST /signup`, `POST /login`, `GET /` — bcrypt 해시, SQLite(`app.db`) + SQLAlchemy ORM
- 회원가입 시 8자리 친구 코드(`friend_code`) 자동 발급 (`main.py`의 `_generate_friend_code`) — 기존 DB에 컬럼이 없으면 서버 시작 시 자동 마이그레이션 + 기존 계정에도 코드 소급 발급 (`_ensure_friend_code_column`)
- 게임 진행 데이터 저장: `GameSave` 모델, `GET/PUT /save` (통짜 JSON blob 저장 방식, 4.3 절 참고)
- 랭킹 API 2종:
  - `GET /leaderboard`: 누적 쌀 소비량(`consumed`) 기준 — **현재 프론트엔드 어디서도 호출하지 않는 사실상 미사용 엔드포인트** (아래 4.7 참고)
  - `GET /leaderboard/weekly`: ISO 주차 기준 `weeklyHarvest`(주간 누적 수확량) 기준, 본인 순위(`myEntry`)도 함께 반환 — 프론트 랭킹 탭이 실제로 사용하는 API
- 친구 API: `Friendship` 모델(양방향을 한 행으로 표현) — `GET /friends/search?code=`, `POST /friends/request`(친구 코드로 요청), `GET /friends/requests`, `POST /friends/accept`, `POST /friends/decline`, `GET /friends`(주간 수확량 내림차순, 게임 LV 포함)
- `game_levels.py`: 프론트 `balance-config.js`의 `GAME_LEVELS`를 그대로 미러링한 서버 사본(친구 목록 응답에 게임 LV 표시용). **밸런스 수정 시 두 파일을 함께 고쳐야 함** — 자동 동기화 장치는 없음
- CORS: `localhost:4174` / `127.0.0.1:4174`만 허용

### 1.2 프론트엔드 핵심 로직 (`frontend/game.js`, `frontend/js/balance-config.js`)
게임이 8/17 시점보다 크게 확장되었다. 레시피 UI 복원 수준이 아니라, 사실상 새로운 성장 시스템이 추가된 상태다.

- **게임 LV 시스템 (1~12, 신규)**: 누적 쌀 소비량 기준. 레벨업마다 고유 보상(비료 부스터 지급, 클릭/자동/전체 수확량 영구 % 보너스, 이펙트 해금 플래그, 부스터 배율 보너스 등) 1회 지급. LV.12 "신농" 달성 시 전용 축하 다이얼로그(`festivalDialog`) 표시 — 기존 기획서의 "작은 쌀 축제 개최" 목표를 대체하는 신규 최종 목표로 보임
- **수확 장비 티어 시스템 (신규)**: 수동/자동 각각 12티어, 티어별 내부 레벨(내부 강화)로 세분화. `basePower = 5^(tier-1)`, 새 티어는 해당 게임 LV 달성 시 해금. 기존 TODO의 "업그레이드 6종" 설명은 더 이상 정확하지 않음
- **비료 부스터 시스템 (신규)**: 게임 LV업 등으로 획득, 사용 시 전체 수확량 배율(기본 2배) 적용, 지속시간은 소모 없이 연장되는 방식(중첩 시 배율은 유지, 시간만 연장). 레시피(밥 짓기/쌀빵 굽기)로 지속시간 보너스 누적
- **레시피 5종**: 기존 그대로 유지 + 수량 선택 UI(`×1/×10/×100/MAX`) 추가. 인기도로 해금, 소비 시 누적 소비량·인기도 증가 + 타임 버프/부스터 지속시간 보너스
- **일일 미션 (`DAILY_MISSIONS`, 신규)**: 클릭/업그레이드/레시피 사용/부스터 사용 4종, 로컬 자정 기준 리셋, 보상은 비료 부스터 또는 인기도(쌀알 직접 지급 없음) — 기존 "미션 5종" 설명을 대체
- **소비 마일스톤 5단계 (기존 유지)**: 통계 탭 전용으로 그대로 존속, 게임 LV 시스템과 별개로 동작
- **주간 랭킹 (신규)**: ISO 주(월요일 시작) 기준 `weeklyHarvest` 추적, 매주 자동 리셋. 예전에 구현했던 "누적 소비량 기준 전체 랭킹"에서 "주간 누적 수확량 기준"으로 설계가 바뀜
- **친구 시스템 (친구 코드 방식으로 변경)**: 아이디 대신 8자리 친구 코드로 요청을 보냄(`/friends/search`, `/friends/request`가 `target_friend_code` 사용). 기존 TODO에 기록된 "아이디로 요청" 방식은 더 이상 코드와 일치하지 않음
- **배경 성장 5단계** + 홈 화면 "벼 성장" 진행바/오늘의 문구(날짜 고정 랜덤 문구 50종) UI 추가
- **오프라인 보상**: 최대 24시간(`OFFLINE_MAX_SECONDS`)으로 확대됨 (기존 TODO 기록은 "최대 4시간" — 값이 바뀌었으니 기획 문서와 대조 필요)
- 계정별 `localStorage` 키 분리(`getStorageKey()`) + 서버 동기화(`syncFromServer`/`pushToServer`) 유지
- 효과음 On/Off(Web Audio 생성 톤), Canvas 논밭 배경 애니메이션, 성장 단계별 배경 전환 — 기존 그대로

### 1.3 하단 네비게이션 (현재 6탭: 홈/업그레이드/미션/통계/랭킹/친구)
- 기존 TODO에 있던 "레시피 탭 별도 신설" 계획과 다르게, **레시피는 별도 탭이 아니라 '업그레이드' 화면 하단에 장비 업그레이드와 함께 통합**되어 있음 (`#upgradeScreen` 안에 `#recipeList`)
- 미션 탭은 이제 "오늘의 미션"(일일 리셋형)이며, 예전에 있던 "미션 5종"과는 다른 시스템
- 통계 탭: 보유 쌀알/누적 소비량/인기도/게임 LV/비료 부스터 요약 + 배경 단계 + 업그레이드 보유 현황 + 소비 마일스톤 — **실제 확인 완료, 정상 동작**
- 랭킹 탭: 주간 누적 수확량 기준, 본인 "(나)" 표시 — **실제 확인 완료, 정상 동작**
- 친구 탭: 내 친구 코드 표시, 코드 입력으로 요청 전송, 받은 요청 수락/거절, 친구 순위표(주간 수확량 기준) — **실제 확인 완료(요청→수락→양쪽 순위표 반영), 정상 동작**

### 1.4 퀵메뉴 4종 (홈 화면) — 상태가 문서와 다름
- **창고**: ~~준비 중 placeholder~~ → **실제 콘텐츠로 구현 완료** (레시피 도감 + 게임 LV 성장 보상 기록). 더 이상 미구현 항목이 아님
- **오늘의 미션**: ~~준비 중 placeholder~~ → **실제 콘텐츠로 구현 완료** (미션 탭과 동일한 목록을 다이얼로그로 표시)
- **이벤트**: 여전히 "준비 중입니다." placeholder — 미구현
- **공지**: 여전히 "준비 중입니다." placeholder — 미구현

### 1.5 인증 화면 / 에셋
- 로그인/회원가입, 세션 저장, 강제 리다이렉트, 로그아웃 — 기존과 동일하게 정상 동작 확인
- 회원가입 성공 메시지에 발급된 친구 코드를 안내하도록 개선되어 있음 (`auth.js`)
- 에셋 목록은 기존 기록과 동일, 추가/누락 없음

## 2. 실제 브라우저 테스트로 확인한 결과 (2026-09-01)

테스트 계정 2개(`todocheck1`/`todocheck2`)를 만들어 아래 흐름을 실제로 조작해 확인했다. 검증 후 두 계정과 관련 데이터는 DB에서 정리했다(`ttest`만 남음).

- 회원가입 → 로그인 → 홈 화면, 콘솔 에러 없음
- 클릭 수확(+1g 반영) → 자동 수확(1g/s 반영) → 장비 내부 강화 구매(Lv.1→2, 비용 차감·생산량 반영) 정상
- 레시피 5종 전부 콘솔로 인기도 주입 후 해금 확인, "밥 짓기" 실제 소비 → 누적 소비량/사용 횟수 반영 정상
- 오늘의 미션: 클릭 횟수·업그레이드 횟수 진행도가 실시간으로 정확히 카운트됨, 완료 시 "받기" → 비료 부스터 지급 정상
- 통계 탭 수치가 홈 화면과 100% 일치
- 랭킹 탭: 주간 수확량 기준 정렬, 본인 "(나)" 표시 정상
- 친구 코드로 요청 전송 → 상대 계정 로그인 후 수락 → 양쪽 친구 순위표에 정확히 반영
- 게임 LV을 12까지 강제로 올려서 확인 — 레벨별 보상(부스터 지급, 영구 보너스 %)이 모두 정확히 누적되고, LV.12 "신농 달성" 다이얼로그가 정상적으로 뜸
- 재로그인 시 서버 저장 기반 오프라인 보상("쉬는 동안 올리가 +N을 수확했어요") 정상 지급

### 2.1 [신규 발견, 중요] 같은 브라우저에서 여러 계정을 동시에 로그인하면 진행 데이터가 서로 덮어써짐
같은 브라우저에서 탭 A(계정1 로그인 상태)를 열어둔 채 탭 B에서 다른 계정(계정2)으로 로그인하면, 두 탭이 같은 오리진(`http://127.0.0.1:4174`)이라 `localStorage["currentUser"]`를 공유한다. 탭 B 로그인 순간 이 키가 계정2로 바뀌는데, 탭 A는 리로드되지 않으므로 메모리상 `state`는 여전히 계정1의 게임 데이터를 들고 있다. 이 상태에서 탭 A의 15초 주기 자동저장(`setInterval(saveState, 15000)`)이나 저장 버튼이 실행되면, `pushToServer()`가 `getCurrentUser()`(=공유 localStorage)를 읽어 **계정1의 데이터를 계정2의 서버 저장(`/save`, user_id=계정2)에 덮어쓴다.**

실제로 재현해서 확인함: 새로 가입한 계정(`todocheck2`, user_id=3)의 서버 저장 데이터가 로그인 직후 다른 계정(`todocheck1`)의 플레이 데이터(일일 미션 진행도, 레시피 사용 횟수 등)로 통째로 바뀌어 있는 것을 `GET /save?user_id=3` 응답으로 확인했다.

- 기존에 문서화된 "user_id를 토큰 검증 없이 신뢰한다"는 위험(4.3/4.6 절 참고)과는 별개의 문제로, **악의적 조작 없이 정상적인 사용(같은 PC에서 다른 계정으로 로그인)만으로도 발생**하는 데이터 유실 버그다.
- [ ] 원인: `getCurrentUser()`가 세션을 탭 단위가 아니라 `localStorage`(오리진 전역)로 관리하기 때문. 근본 해결에는 로그인 인증/세션 구조 자체의 변경이 필요해 보이며, 우선순위와 해결 방향(예: 세션을 `sessionStorage`로 바꾸기 — 탭별 격리는 되지만 새 탭에서 로그인 유지가 끊기는 트레이드오프 있음) 사용자 확인 필요

### 2.2 출석 보상 기능 추가 후 전체 기능 회귀 테스트 (2026-09-01, 2차)

출석 보상(3.1절) 구현이 기존 기능을 깨뜨리지 않았는지 확인하기 위해, 새 테스트 계정 2개(`regtest1`/`regtest2`)로 처음부터 끝까지 다시 조작해서 확인했다. 검증 후 두 계정과 관련 데이터는 DB에서 정리했다(`ttest`만 남음).

- 회원가입 → 로그인 → 홈 화면, 콘솔 에러 없음
- **퀵메뉴 5개 전부**(창고/이벤트/공지/오늘의 미션/**출석**) 하나씩 열어봄 — 5번째 버튼이 추가됐지만 레이아웃이 깨지거나 다른 버튼과 겹치지 않음. 창고(레시피 도감+성장 보상 기록)·오늘의 미션·출석은 실제 콘텐츠 정상 표시, 이벤트·공지는 여전히 "준비 중" placeholder로 그대로임
- 클릭 수확 / 자동 수확 정상
- 업그레이드 탭: 장비 내부 강화 구매(비용 차감·생산량 반영), 레시피 5종 해금 후 "밥 짓기" 소비(누적 소비량·사용 횟수 반영) 정상
- 미션 탭: 클릭/업그레이드/레시피 진행도 실시간 카운트 정상, 완료 시 "받기" → 비료 부스터 지급 정상
- 통계 탭 수치가 홈 화면과 100% 일치
- 랭킹 탭: 주간 수확량 기준 정렬 정상
- 친구 기능: 계정2 → 계정1에게 친구 코드로 요청 전송 → 계정1 로그인 후 수락 → 양쪽 친구 순위표에 정확히 반영
- 게임 LV을 콘솔로 12까지 올려서 재확인 — 레벨별 보상 누적, LV.12 "신농 달성" 다이얼로그 정상적으로 뜸 (출석 코드가 같은 `checkDailyAndWeeklyReset()` 경로를 타서 특히 중점적으로 재확인한 부분)
- 콘솔 에러 없음 (테스트 전체 과정)

**결론: 출석 보상 추가로 인한 회귀(기존 기능 손상)는 발견되지 않았다.**

검증 중 발견한 사항 (버그 아님, 참고용 기록):
- 로그아웃 버튼 클릭이 브라우저 자동화 도구의 타이밍 문제로 한 번 씹혀서, 로그인 화면으로 안 넘어가고 이전 계정 화면에 그대로 머문 적이 있었다. 그 상태에서 다른 계정 로그인 폼을 입력해도 실제로는 아무 일도 일어나지 않아 일시적으로 "진행 데이터가 사라진 것처럼" 보였는데, `GET /save`로 서버 데이터를 직접 조회해서 실제 진행 데이터는 멀쩡히 저장되어 있었음을 확인했다. 앱 코드와는 무관.
- 이번 테스트에도 동일 브라우저 프로필을 재사용하면서 SQLite가 삭제된 계정의 rowid를 재사용해, 신규 가입 계정이 과거 테스트에서 남은 `localStorage` 값을 그대로 불러오는 현상이 반복됐다(2.1절 멀티탭 버그와는 별개로, 단일 탭·단일 세션에서도 발생). `resetGame()`으로 초기화 후 진행해 테스트에는 지장 없었지만, 실제 서비스 환경(연속된 정수 ID가 프론트 `localStorage` 키로 그대로 쓰이는 구조)에서도 이론상 동일한 위험이 있다는 점은 2.1절과 함께 참고할 만하다.

## 3. TODO — 아직 구현되지 않은 것

### 3.1 출석 보상 시스템 (기획서 12.7) — [완료, 2026-09-01]
- [x] 7일 출석판 UI 신규 구현 — 퀵메뉴에 5번째 버튼 '출석'을 새로 추가(`index.html`의 `.quick-menu`, `attendanceDialog`)해서 배치 문제 해결. 창고/이벤트/공지/오늘의 미션 4자리를 건드리지 않고 독립된 진입점으로 분리
  - `frontend/js/balance-config.js`: `ATTENDANCE_REWARDS` 7일치 보상 테이블 신설 — 1·2·5일차는 쌀알(30/80/200g, 초반 체감용), 3·6일차는 비료 부스터(+1/+2), 4일차는 인기도(+8), 7일차(마지막)는 비료 부스터 +3 + 인기도 +15 콤보로 마무리
  - `frontend/game.js`: `state.attendance = { count, lastDateKey, claimedDays }` 저장 상태 추가(로컬+서버 저장 대상에 포함, `normalizeState`에서 병합), `checkAttendanceProgress()`(로컬 날짜가 바뀔 때마다 1일씩 증가, 연속 접속 요구 없음, 7일 도달 후 더 증가하지 않음 — 반복 없는 1회성 출석판), `claimAttendanceReward(day)`, `renderAttendance()` 구현. 미인식 보상 여부를 `.quick-menu-btn.has-badge`로 빨간 점 표시
  - [x] 출석 기록 상태를 저장 데이터(로컬 및 서버)에 추가 — 완료. 기존 `pushToServer`/`syncFromServer`가 `state` 전체를 그대로 직렬화하는 구조라 별도 API 변경 없이 자동으로 포함됨
  - 브라우저에서 신규 계정으로 실제 확인: 로그인 시 "출석 1/7일차" 토스트 → 1일차 받기(쌀알 +30g 반영) → 콘솔로 2~7일차 진행시켜 전부 수령(쌀알/부스터/인기도 합산치 정확히 일치) → 7일 완료 후 배지 사라짐과 8일차 이상 증가하지 않음까지 확인. 콘솔 에러 없음. 테스트 계정은 정리함

### 3.2 퀵메뉴 placeholder 2종 — 여전히 미구현
- [ ] '이벤트' 다이얼로그 실제 내용 정의 및 구현 — 기획서에 없는 개념, 용도 확인 필요
- [ ] '공지' 다이얼로그 실제 내용 정의 및 구현 — 기획서에 없는 개념, 출석 보상과 별개로 유지할지 확인 필요
- (참고: '창고'와 '오늘의 미션'은 이번 검증에서 이미 실제 콘텐츠로 구현되어 있는 것을 확인했으므로 더 이상 이 목록에 없음)

### 3.3 기타 기술 이슈
- [ ] [신규, 2.1절] 멀티탭/멀티계정 동시 로그인 시 서버 저장 데이터가 서로 덮어써지는 문제 — 세션 관리 구조 개선 필요
- [ ] `GET /leaderboard`(누적 소비량 기준) 엔드포인트가 프론트 어디에서도 호출되지 않는 사실상 죽은 코드로 확인됨 — 의도적으로 남겨둔 것인지, 정리해야 할 레거시인지 확인 필요
- [ ] `game_levels.py`(백엔드)와 `balance-config.js`(프론트)의 `GAME_LEVELS` 값이 수동으로 두 곳에 복제되어 있음 — 밸런스 수정 시 한쪽만 고치면 친구 목록의 게임 LV 표시가 실제 게임 화면과 어긋날 수 있음 (당장 버그는 아니지만 유지보수 리스크로 기록)
- [x] ~~`backend/requirements.txt`가 UTF-16으로 저장되어 인코딩이 깨져 있다~~ → 재확인 결과 ASCII(CRLF)로 정상 저장되어 있음, 이미 해결됨
- [x] ~~`frontend/js/auth.js`의 회원가입/로그인 에러 메시지가 `[object Object]`로 표시되는 버그~~ → 코드에 `formatErrorDetail()` 처리가 이미 추가되어 있고, 실제 코드 주석에도 "기존 TODO.md 4.7에 기록된 버그 수정"이라고 명시되어 있음. 이미 해결됨

### 3.4 사용자 확인을 거쳐 보류/제외 확정된 항목 (재검토 불필요)
- [x] 작은 쌀 축제 목표 카드(`bottom-cards`) — HTML에 `hidden` 속성이 하드코딩되어 있고 "요청에 따라 비활성화"라는 주석이 남아 있음. 이미 결정되어 반영된 상태이므로 더 이상 "재검토 대기" 항목이 아님 (게임 LV.12 신농 달성 다이얼로그가 사실상 이 역할을 대체하는 것으로 보임)
- [x] 실제 사운드 파일(음원) 적용 제외 — Web Audio 생성 톤 계속 사용 (기존 결정 유지)

## 4. 완료된 것으로 재확인된 과거 TODO 항목 (요약)
아래는 2026-08-17 기록 시점에 이미 완료 표시되어 있었고, 이번 재검증에서도 실제 코드·브라우저 동작으로 다시 확인된 항목이다. 다만 세부 구현(레벨/장비/부스터/미션/랭킹 방식)은 위 1장 기준이 최신이다.

- [x] 레시피 소비 → 누적 소비량/인기도 상승 전체 루프
- [x] 하단 네비게이션 정리 (단, 구성은 위 1.3절 기준으로 갱신됨: 레시피 탭 대신 업그레이드 화면에 통합)
- [x] 통계 화면
- [x] 랭킹 기능 (단, 집계 기준이 "누적 소비량" → "주간 누적 수확량"으로 변경됨)
- [x] 친구 기능 (단, 검색 방식이 "아이디" → "친구 코드"로 변경됨)
- [x] 게임 데이터 서버 저장/동기화 (계정별 localStorage 키 분리, 오프라인 보상 등 포함)

## 5. 명시적으로 이번 범위에서 제외
- 실제 사운드 파일(음원) 적용 — 현재 Web Audio로 생성하는 간단한 톤 유지

## 6. 다음에 확인이 필요한 것 (제안)
- [ ] 2.1절 멀티탭 세션 공유 버그의 해결 방향(세션 저장 방식 변경 여부) 사용자 확인
- [x] ~~출석 보상 UI의 배치 위치~~ → 퀵메뉴에 5번째 버튼으로 신설해 해결 (3.1절 참고)
- [ ] '이벤트'/'공지' 다이얼로그의 실제 용도 확정
- [ ] `GAME_DESIGN.md`가 여전히 "누적 소비량 기준 랭킹", "작은 쌀 축제" 등 옛 설계를 기준으로 서술되어 있어, 실제 구현(주간 랭킹, 게임 LV.12 신농 달성)과 괴리가 있음 — 기획서를 실제 구현에 맞춰 갱신할지, 반대로 기획서 의도대로 되돌릴지 방향 확인 필요

---

## 7. 부록 — 2026-08-17 시점 기록 (과거 결정 사항 보존용)

> 아래는 이전 버전 문서의 원문이다. 당시의 의사결정 맥락(왜 그렇게 만들었는지)은 여전히 유효한 참고 자료이므로 삭제하지 않고 보존한다. 단, "지금 무엇이 구현되어 있는가"에 대한 최신 정보는 위 1~4장을 따른다.

### 7.1 사용자 확인 결과 (2026-08-17)

| 항목 | 결정 |
| --- | --- |
| 레시피/소비량/인기도 UI | 복원·재구현 필요 (최우선) |
| 랭킹/친구 탭 | 실제 기능 구현 예정 (기획서에 없던 신규 범위이므로 별도 기획 확정 필요) |
| 서버 저장 연동 | 포함 — 계정 기준으로 게임 데이터 저장/동기화 구현 |
| 출석 보상 | 포함 |
| 통계 화면 | 포함 |
| 실제 사운드 파일 | 제외 (Web Audio 생성 톤 유지) |

### 7.2 최종 통합 검증 (2026-08-17)

4장에서 완료 표시한 기능들을 새 테스트 계정 2개(`finalcheck1`, `finalcheck2`)로 처음부터 끝까지 이어서 재검증했다. 확인한 흐름:

- 회원가입 → 로그인 → 홈 화면 진입, 콘솔 에러 없음
- 클릭 수확 → 첫 수확 미션 달성/수령 → 업그레이드(좋은 볍씨) 구매 후 초당 수확 반영 → 레시피(밥 짓기) 실행 → 누적 소비량·인기도 갱신
- 통계 탭에서 보유 쌀알/누적 소비량/인기도/레벨·경험치/업그레이드 보유 현황/마일스톤 진행률이 홈·레시피 탭과 일치하는지 확인
- 랭킹 탭에서 본인이 누적 소비량 기준으로 정렬되어 표시되고 "(나)" 표시 확인
- 친구 탭에서 실제 아이디로 요청 전송 → 상대 계정으로 로그인 후 수락 버튼 클릭 → 양쪽 모두 친구 순위표에 정확한 소비량으로 반영
- 저장 후 `localStorage` 초기화 + 재로그인(새 기기 가정)으로 서버에서 진행 데이터가 정확히 복원되는지 확인 (오프라인 경과 시간만큼 쌀알 추가 반영도 정상)

검증 중 발견한 사항 (버그 아님, 참고용 기록):
- 저장(`저장` 버튼) 직후 0.1~0.2초 이내에 랭킹/친구 화면을 열면, `pushToServer()`가 비동기(fire-and-forget)라 서버 반영이 완료되기 전이라 일시적으로 이전 값이 보일 수 있다. 곧바로 다시 열면 정상적으로 최신 값이 표시된다.
- 테스트 중 브라우저 자동화 도구 자체의 일시적인 렌더링 지연으로 첫 클릭이 반영되지 않는 것처럼 보인 경우가 있었으나, 재클릭 시 정상 동작하고 실제 앱 코드와는 무관한 것으로 확인했다.

검증에 사용한 테스트 계정과 데이터는 모두 정리했으며, 당시 DB에는 원래 있던 실제 계정(`ttest`, id 1)만 남아 있었다.
